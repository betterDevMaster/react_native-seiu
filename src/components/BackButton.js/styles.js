import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginTop: 28,
    marginBottom: 20,
    height: 44,
    textAlignVertical: 'center',
  },
  image: {
    width: 20,
    height: 14,
  },
});

import {StyleSheet} from 'react-native';
import {colors} from '../../../utils/colors';

export const styles = StyleSheet.create({
  container: {},
  switchContainer: {padding: 20},
  switchText: {
    color: colors.black,
    fontSize: 15,
  },
});
